{# Defines specific variables #}
{% set title = 'label.reset_content'|trans({}, 'user') %}

{{ form_start(form, { 'action': path('fos_user_resetting_reset', {'token': token}), 'attr': { 'class': 'fos_user_resetting_reset' } }) }}
    {{ form_widget(form) }}

    <div class="form-group">
        <input type="submit" value="{{ 'resetting.reset.submit'|trans({}, 'FOSUserBundle') }}" class="btn btn-block btn-lg btn-primary" />
    </div>
{{ form_end(form) }}
